import React from 'react'

const Devices = ({devices,devices1}) => {
  return (
      <div className='devices'>
    <div>{devices}</div>
    <div>{devices1}</div>
    </div>
  )
}

export default Devices